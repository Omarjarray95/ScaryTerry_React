import { combineReducers } from 'redux';
import widgetPunc from './widgets.reducer';
//import widgetTest from './widgets.reducer';

const reducer = combineReducers({
    widgetPunc
});

export default reducer;
